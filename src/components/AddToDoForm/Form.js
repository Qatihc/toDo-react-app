/* import React from 'react';

let Form = function(props) {
  let {inputList} = props;
  return(
    <div>
      <form>
        {inputList.map(input =>
          <input 
            type={input.type || 'text'}
            
          />
        )}
      </form>
    </div>
  )
}

export default Form; */